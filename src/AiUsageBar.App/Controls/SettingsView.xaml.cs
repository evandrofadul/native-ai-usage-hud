using System.Windows.Controls;

namespace AiUsageBar.App.Controls;

/// <summary>Interaction logic for SettingsView.xaml — the in-window settings dialog body.</summary>
public partial class SettingsView : UserControl
{
    public SettingsView() => InitializeComponent();
}
